@using CarrierConstruct.Blazor.Enums
@using CarrierConstruct.Blazor.Interfaces
<MudPaper Width="300px">
    <MudText Typo="Typo.h6">Hangar</MudText>

    @foreach (var aircraft in AircraftInHangar)
    {
        <div class="pa-4" @onclick="(args) => SelectAircraft(args, aircraft)">
            <div class="d-flex">
                @if (selectedAircraft.Contains(aircraft))
                {
                    <MudPaper Class="mud-theme-primary">
                        <MudIcon Icon="@Icons.Material.Filled.ArrowCircleUp" Title="Favorite" />
                    </MudPaper>
                }
                <MudText Typo="Typo.body2">@aircraft.FullName</MudText>
            </div>
        </div>
    }
    
    <MudButton Variant="Variant.Filled"
               Color="Color.Primary"
               FullWidth="true"
               @onclick="OrderSelectedAircraftToFlightDeck">
        Order Selected Aircraft to Flight Deck
    </MudButton>
    
    <div>
        @foreach (IAircraft a in selectedAircraft)
        {
            <p>@a.Name</p>
        }
    </div>
    
</MudPaper>